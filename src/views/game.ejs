<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>Quiz Buzzer – <%= username %>
	</title>

	<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@500&display=swap" rel="stylesheet">

	<style>
		:root {
			--bg: #fffaf2;
			--text: #333;
			--primary: #ff9f1c;
			--accent: #2ec4b6;
			--soft: #f4f4f4;
			--tap-red: #ff6b6b;
		}

		body {
			background-color: var(--bg);
			color: var(--text);
			font-family: 'Fredoka', sans-serif;
			display: flex;
			flex-direction: column;
			align-items: center;
			justify-content: flex-start;
			padding: 2rem 1rem;
			min-height: 100vh;
			margin: 0;
			text-align: center;
		}

		h1 {
			font-size: 2.5rem;
			color: var(--primary);
			margin-bottom: 0.3em;
		}

		#status {
			font-size: 1.3rem;
			color: var(--accent);
			margin-bottom: 1em;
		}

		#playerList {
			font-size: 1.1rem;
			color: #666;
			margin-bottom: 1.5em;
		}

		#startBtn {
			background-color: var(--accent);
			color: #fff;
			border: none;
			font-size: 1.1rem;
			padding: 0.75rem 1.8rem;
			border-radius: 12px;
			box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
			cursor: pointer;
			transition: background-color 0.2s ease;
			margin-bottom: 2rem;
		}

		#startBtn:hover {
			background-color: #25b0a6;
		}

		#buzzerContainer {
			margin-top: 1rem;
		}

		#tapBtn {
			width: 160px;
			height: 160px;
			border-radius: 50%;
			background-color: var(--tap-red);
			border: none;
			box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
			color: #fff;
			font-size: 2rem;
			font-weight: bold;
			cursor: pointer;
			transition: all 0.2s ease;
		}

		#tapBtn:hover:not(:disabled) {
			transform: scale(1.05);
		}

		#tapBtn:active:not(:disabled) {
			transform: scale(0.95);
		}

		#tapBtn:disabled {
			background-color: #ffc5c5;
			color: #aaa;
			cursor: not-allowed;
			box-shadow: none;
		}

		footer {
			margin-top: 3rem;
			font-size: 0.9rem;
			color: #aaa;
		}
	</style>
</head>

<body>
	<h1>Welcome, <%= username %> 🎉</h1>

	<div id="status">Connecting to the fun center...</div>
	<div id="playerList">Loading players…</div>

	<button id="startBtn">Start the Show 🎬</button>

	<div id="buzzerContainer">
		<button id="tapBtn" disabled>🔔 BUZZ</button>
	</div>

	<footer>Quick on the buzz = bragging rights for life. (Probably.)</footer>

	<script>
		const username = "<%= username %>";
		const protocol = window.location.protocol === "https:" ? "wss:" : "ws:";
		const ws = new WebSocket(`${protocol}//${window.location.host}/?username=${encodeURIComponent(username)}`);

		const status = document.getElementById('status');
		const playerList = document.getElementById('playerList');
		const tapBtn = document.getElementById('tapBtn');
		const startBtn = document.getElementById('startBtn');

		ws.onopen = () => {
			status.innerText = "✅ Connected!";
		};

		ws.onmessage = (event) => {
			const data = JSON.parse(event.data);

			switch (data.type) {
				case 'gameState':
					status.innerText = `🎮 Game state: ${data.state}`;
					break;
				case 'players':
					playerList.innerText = `👥 Players: ${data.players.join(', ')}`;
					break;
				case 'gameStart':
					status.innerText = `Get ready... Countdown: ${data.countdown}`;
					startBtn.disabled = true;
					break;
				case 'countdown':
					status.innerText = `⏳ Starting in... ${data.count}`;
					break;
				case 'gameActive':
					status.innerText = `🚨 GO! Buzz now!`;
					tapBtn.disabled = false;
					break;
				case 'winner':
					tapBtn.disabled = true;
					status.innerText = `🏆 ${data.winner} buzzed first at ${data.timestamp}ms!`;
					startBtn.disabled = false;
					break;
				case 'gameReset':
					status.innerText = `🔁 Game reset. Ready when you are!`;
					tapBtn.disabled = true;
					startBtn.disabled = false;
					break;
				case 'error':
					alert(data.message);
					window.location.href = '/';
					break;
			}
		};

		tapBtn.addEventListener('click', () => {
			const timestamp = performance.now().toFixed(1);
			ws.send(JSON.stringify({
				type: 'tap',
				username,
				timestamp
			}));
			tapBtn.disabled = true;
		});

		startBtn.addEventListener('click', () => {
			ws.send(JSON.stringify({type: 'startGame'}));
			startBtn.disabled = true;
		});
	</script>
</body>

</html>
